[package]
name = "evidence-registry"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[workspace]
members = ["agent-evidence-binder"]
resolver = "2"

[dependencies]
sha2 = "0.10.9"

[target.'cfg(any(target_os = "linux", target_os = "macos"))'.dependencies]
libc = "0.2"
