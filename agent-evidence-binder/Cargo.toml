[package]
name = "ai-agent-evidence-binder"
version = "0.3.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
evidence-registry = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10.9"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
